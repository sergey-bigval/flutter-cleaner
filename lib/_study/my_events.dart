class MyEvent {
  String name;
  String location;
  DateTime startDateTime;

  MyEvent({required this.name, required this.location, required this.startDateTime});
}

// var e = MyEvent(location: "cxvx", name: "xvcxzcv", startDateTime: DateTime.now());
